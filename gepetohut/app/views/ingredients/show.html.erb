<p id="notice"><%= notice %></p>

<div class="container">
  <div class="panel panel-default">
    <div class="panel-heading">
      <h2 class="panel-title">Dados da Pizza</h2>
    </div>
    <div class="panel-body">
      <p>
        <strong>Name:</strong>
        <%= @ingredient.name %>
      </p>

      <p>
        <strong>Price:</strong>
        <%= @ingredient.price %>
      </p>
    </div>
  </div>
</div>

<div class="container">
  <%= button_to "Editar Ingrediente", edit_ingredient_path(@ingredient), method: :get, class: "btn btn-primary" %>
  <%= link_to raw( "Voltar" ), ingredients_path, class: "btn btn-primary", style: "color: white" %>
</div>
