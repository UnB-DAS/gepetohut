<%= form_for(@pizza) do |f| %>
  <% if @pizza.errors.any? %>
    <div class="alert alert-danger">
      <% @pizza.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="panel panel-default">
    <div class="panel-heading">
      <%= f.label "Nome" %><br>
    </div>
    <div class="panel-body">
      <%= f.text_field :name, size: "50", class: "form_control", id: "name-field" %><br>
    </div>
  </div>

  <div class="panel panel-default">
    <div class="panel-heading">
      <%= f.label "Preço" %><br>
    </div>
    <div class="panel-body">
      <%= f.text_field :price, size: "50", class: "form_control", id:"price-field" %><br>
    </div>
  </div>

  <div class="actions">
    <%= f.submit "Atualizar Pizza", class: "btn btn-success" %>
  </div>
<% end %>
