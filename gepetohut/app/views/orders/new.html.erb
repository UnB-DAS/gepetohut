<div class="container">
  <h1>Novo Pedido</h1>
</div>

<div class="container">
  <%= render 'new_order_form' %>
</div>

<div class="span7 text-center" class="container">
  <% if can? :read, Order %>
    <%= link_to raw( "Voltar" ), orders_path, class: "btn btn-primary", style: "color: white" %>
  <% else %>
    <%= link_to raw( "Voltar" ), home_page_index_path, class: "btn btn-primary", style: "color: white" %>
  <% end %>
</div>
